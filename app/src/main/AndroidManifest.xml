<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <queries>
        <package android:name="com.google.android.apps.healthdata" />
        <!-- Permet de détecter les autres apps de santé -->
        <intent>
            <action android:name="android.health.ACTION_SHOW_HEALTH_CONNECT" />
        </intent>
    </queries>

    <!-- Permissions Caméra & Internet -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Permissions Santé Connect (READ & WRITE) -->
    <uses-permission android:name="android.permission.health.READ_WEIGHT" />
    <uses-permission android:name="android.permission.health.WRITE_WEIGHT" />
    <uses-permission android:name="android.permission.health.READ_STEPS" />
    <uses-permission android:name="android.permission.health.WRITE_STEPS" />
    <uses-permission android:name="android.permission.health.READ_HEART_RATE" />
    <uses-permission android:name="android.permission.health.WRITE_HEART_RATE" />
    <uses-permission android:name="android.permission.health.READ_NUTRITION" />
    <uses-permission android:name="android.permission.health.WRITE_NUTRITION" />
    <uses-permission android:name="android.permission.health.READ_HYDRATION" />
    <uses-permission android:name="android.permission.health.WRITE_HYDRATION" />
    <uses-permission android:name="android.permission.health.READ_DISTANCE" />
    <uses-permission android:name="android.permission.health.WRITE_DISTANCE" />
    <uses-permission android:name="android.permission.health.READ_CALORIES_BURNED" />
    <uses-permission android:name="android.permission.health.WRITE_CALORIES_BURNED" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Fit AI Tracker"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Material.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ContentProvider pour partager les données de santé -->
        <provider
            android:name=".provider.HealthDataProvider"
            android:authorities="com.fitai.tracker.health"
            android:exported="true"
            android:permission="android.permission.health.READ_WEIGHT">
            <path-permission
                android:path="/weight"
                android:readPermission="android.permission.health.READ_WEIGHT"
                android:writePermission="android.permission.health.WRITE_WEIGHT" />
            <path-permission
                android:path="/steps"
                android:readPermission="android.permission.health.READ_STEPS"
                android:writePermission="android.permission.health.WRITE_STEPS" />
            <path-permission
                android:path="/heart_rate"
                android:readPermission="android.permission.health.READ_HEART_RATE"
                android:writePermission="android.permission.health.WRITE_HEART_RATE" />
        </provider>

    </application>
</manifest>
